apiVersion: chaos-mesh.org/v1alpha1
kind: NetworkChaos
metadata:
  name: iot-producer-bandwidth
spec:
  action: bandwidth
  mode: all
  selector:
    namespaces: [default]
    labelSelectors:
      app: iot-producer
  direction: to
  bandwidth:
    rate: "2mbps"